<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.herusantoso.tokonezia.tokonezia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionTheme">
        <activity android:name=".view.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.SignUpActivity" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.ProductActivity" />
        <activity android:name=".view.OrderActivity" />
        <activity
            android:name=".view.ChangeProfileActivity"
            android:label="Sign Up" />
        <activity android:name=".view.ChangePasswordActivity" />
        <activity android:name=".view.ShopActivity"></activity>
    </application>

</manifest>